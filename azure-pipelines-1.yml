pool:
  vmImage: 'ubuntu-latest'

variables:
  tier: web
  target: dev

steps:
- bash:
    echo $(tier)
- bash:
    echo $(target)

  displayName: 'regular variable'

- bash:
    echo $(TIER)
- bash:
    echo $(TARGET)

  displayName: 'environment variable'
